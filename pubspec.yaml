name: agenda_academica_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  cupertino_icons: ^1.0.8
  equatable: ^2.0.7
  firebase_core: ^3.10.1
  firebase_messaging: ^15.2.1
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  flutter_html: ^3.0.0-alpha.5
  http: ^1.3.0
  image_picker: ^1.1.2
  provider: ^6.1.2
  shared_preferences: ^2.5.1
  speech_to_text: ^7.0.0
  table_calendar: ^3.2.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
